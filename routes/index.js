var express = require('express');
var router = express.Router();

var user = require('../controller/usercontroller');

/* GET home page. */

.0
router.get('/data',user.register_user);
router.get('/start',user.start_user);

module.exports = router;
